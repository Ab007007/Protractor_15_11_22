let fn = "Aravinda"
let address = new String("Bangalore, BTM Layout ")

console.log(`${fn} stays in ${address}`);


console.log(`Char At index 2 ${address.charAt(2)}`);
console.log(`Index of Char - B, ${address.indexOf('B')}`);
console.log(`index of Char from index 2 , ${address.indexOf('B',2)}`);
console.log(`Last index of char - B, ${address.lastIndexOf('B')}`);
console.log(`Length of the String ${address.length}`);
console.log(`Trim to remove spaces, ${address.trim().length}`);
console.log(`To Upper Case ${address.toUpperCase()}`);
console.log(`To Lower Case ${address.toLowerCase()}`);

